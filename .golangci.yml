version: "2"
linters:
  enable:
    - misspell
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
formatters:
  enable:
    - gofmt
    - goimports
  settings:
    gofmt:
      simplify: true
    goimports:
      local-prefixes:
        - github.com/pintomau/ringring
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
